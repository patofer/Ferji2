package com.ferji.inspecciones.di

/**
 * Los ViewModels se inyectan automáticamente gracias a @HiltViewModel,
 * por lo que este módulo no es necesario. Se mantiene como punto de
 * extensión si en el futuro se requiere proveer dependencias asistidas
 * o factories personalizadas para ViewModels.
 */
// Si no se necesita, se puede eliminar este archivo por completo.

